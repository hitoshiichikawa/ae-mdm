// 共通ライブラリ（api-client / oidc / queryClient）
// 本 Issue では placeholder のみ。実装は umbrella task 12.1 で追加。

/**
 * api-client / oidc / queryClient のエクスポート placeholder。
 * 後続 Issue で本 placeholder を本実装に置き換える。
 */
export const SHARED_LIB_PLACEHOLDER = '@ae-mdm/shared/lib (Issue #1 scaffold)';
