# sqlc 生成設定（Issue #1 では scaffold のみ。実 query は umbrella Task 2.3 で追加）
version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "sqlcgen"
        out: "internal/platform/db/sqlcgen"
        sql_package: "pgx/v5"
